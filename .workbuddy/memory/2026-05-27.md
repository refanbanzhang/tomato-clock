# 2026-05-27

## 功能清理
- 移除了「测试完成」按钮（TimerControls.tsx 及 page.tsx 中的 handleTestComplete）
- 移除了「清除今天」「清除所有」按钮（SettingsPanel.tsx 及 page.tsx 中的 handleClearToday/handleClearAll）
- 清理了不再使用的 uploadState 导入
- 构建验证通过

## 修复
- 设置弹层按 ESC 无响应：在 page.tsx 中添加 useEffect，当 showSettings 为 true 时监听 Escape 键关闭弹层
