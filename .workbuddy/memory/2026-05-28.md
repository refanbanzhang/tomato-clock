# 2026-05-28

## Landing Page 重设计

- 通过 brainstorming skill 完成了完整的设计流程（风格选择 → 布局确认 → 设计文档）
- 选定"卡片叠层 · 精致阴影"风格，slate 基底配色
- 新增 Section 3 Demo 截图区（Hero 之后，2 列占位）
- 通过 subagent-driven-development 完成了 8 个 task 的实现：
  - Tasks 1-5: 5 个 locale 文件追加 6 个 i18n key
  - Task 6: globals.css 新增 slate 色板 + .landing-card/.landing-icon-box/.landing-demo-frame 工具类
  - Task 7: 重写 app/landing/page.tsx，7-section 结构
  - Task 8: 构建验证 + JSON 格式检查，全部通过

### 关键设计决策
- 图标：沿用原有内联 SVG（非 emoji）
- CTA 主按钮：深色 slate-900 替代 orange-500
- Stats 背景：slate-900 替代 teal-600
- Nav brand 使用 `landing_brand`，Footer brand 使用 `landing_footer_brand`
- Demo 占位框使用翻译 key 填充（避免硬编码）

### 提交
- e0fba58: spec 文档
- 3b6c33d: 图标澄清
- 3b92268: 实现计划
- e480efd..cb40576: i18n keys (5 commits)
- 23e9519: globals.css
- 46fc553: landing page 重写
- 544c416: bug 修复（demo label + footer brand）
- 98d6aa5: Demo 区截图替换为实际产品截图（计时器 + 日历）

### Landing Page 主题切换
- 在 nav 栏添加 ThemeToggle 按钮，位于 LanguageSwitcher 左侧
- 使用 `next/dynamic` + `ssr: false` 导入 ThemeToggle，避免静态导出预渲染时因 ThemeProvider 上下文缺失报错
- 构建通过，landing page 现已支持亮/暗主题一键切换

## Favicon + iOS 主屏幕图标

- 生成 `apple-touch-icon.png`（180×180，橙色圆角底 + 番茄 Logo）
- 生成 `favicon-32.png` / `favicon-16.png`
- 使用 Next.js 文件约定：`app/icon.png` + `app/apple-icon.png`（自动处理 basePath）
- 添加 `manifest.json`（PWA 支持）
- `metadata.appleWebApp` 声明独立应用模式

## iOS 灵动岛安全区适配

- 最初加 `viewport-fit=cover` 导致顶部空白过大
- 最终方案：去掉 `viewport-fit=cover`，恢复默认安全区行为
- `.page-tools` fixed 定位保留 `env(safe-area-inset-top)`，确保按钮不被灵动岛遮挡
- `.page` padding 恢复原始值，避免过度留白
